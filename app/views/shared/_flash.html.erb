<% if flash.any? %>
  <div class="flash">
    <% flash.each do |type, message| %>
      <% next if message.blank? %>

      <div class="alert alert--<%= type %>" role="alert">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
